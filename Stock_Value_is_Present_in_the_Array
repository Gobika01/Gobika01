-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Write a C program to check if the searched stock value is present in the array. If the stock value is found, display how many times it appears; otherwise, indicate that the stock value is not available.
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#include <stdio.h>
int main() {
     printf("\n~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~");
     printf("\n Program by : Gobika.N - I Year B.E CSBS"); 
     printf("\n~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~");
    int stock[10], searchValue, count = 0;
    printf("Enter the stock of 10 items:\n");
    for (int i = 0; i < 10; i++) {
        scanf("%d", &stock[i]);
    }
    printf("Enter the stock value to search: ");
    scanf("%d", &searchValue);
    for (int i = 0; i < 10; i++) {
        if (stock[i] == searchValue) count++;
    }
    if (count > 0)
        printf("The stock value %d is present %d times.\n", searchValue, count);
    else
        printf("The stock value %d is not available.\n", searchValue);
        
    return 0;
}
Output
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Program by : Gobika.N - I Year B.E CSBS
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Enter the stock of 10 items:
7
5
45
78
52
14
96
32
14
5
Enter the stock value to search: 5
The stock value 5 is present 2 times.
``````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````

